# 벡터 저장소 필요성 샘플 프로젝트 실행 가이드

이 가이드는 6.1.VectorDB 샘플 프로젝트를 실행하고 테스트하는 방법을 안내합니다.

## 📋 사전 준비사항

- JDK 17 이상 설치
- OpenAI API Key 발급
- 환경 변수 설정 권한

## 🚀 실행 방법

### 1. 환경 변수 설정

터미널에서 API Key를 환경 변수로 설정:

**macOS / Linux:**
```bash
export OPENAI_API_KEY="sk-your-api-key-here"
```

**Windows (PowerShell):**
```powershell
$env:OPENAI_API_KEY="sk-your-api-key-here"
```

### 2. 프로젝트 실행

```bash
./gradlew bootRun
```

또는 IntelliJ IDEA에서 `VectorDBApplication.kt` 실행

### 3. 실행 확인

애플리케이션이 정상적으로 시작되면 다음과 같은 메시지가 표시됩니다:

```
Started VectorDBApplication in X.XXX seconds
```

## 🧪 API 테스트

### 기본 문서 추가 및 검색

#### 1. 문서 추가

```bash
curl -X POST http://localhost:9000/api/vectordb/add \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Spring AI는 프레임워크입니다.",
    "metadata": {"category": "framework"}
  }'
```

**예상 응답:**
```json
{
  "status": "success",
  "message": "문서가 추가되었습니다.",
  "content": "Spring AI는 프레임워크입니다.",
  "metadata": {
    "category": "framework"
  }
}
```

#### 2. 여러 문서 추가

```bash
curl -X POST http://localhost:9000/api/vectordb/batch/add \
  -H "Content-Type: application/json" \
  -d '{
    "texts": [
      "코딩은 프로그래밍의 일부입니다.",
      "개발은 소프트웨어를 만드는 과정입니다.",
      "요리는 음식을 만드는 기술입니다."
    ],
    "source": "manual"
  }'
```

#### 3. 유사도 검색 (POST)

```bash
curl -X POST http://localhost:9000/api/vectordb/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "프로그래밍",
    "topK": 5
  }'
```

**예상 응답:**
```json
{
  "query": "프로그래밍",
  "topK": 5,
  "resultCount": 2,
  "results": [
    {
      "rank": 1,
      "content": "코딩은 프로그래밍의 일부입니다.",
      "metadata": {
        "index": 0,
        "source": "manual",
        "addedAt": 1234567890
      },
      "id": "unknown"
    },
    {
      "rank": 2,
      "content": "개발은 소프트웨어를 만드는 과정입니다.",
      "metadata": {
        "index": 1,
        "source": "manual",
        "addedAt": 1234567890
      },
      "id": "unknown"
    }
  ]
}
```

#### 4. 유사도 검색 (GET)

```bash
curl "http://localhost:9000/api/vectordb/search?query=프로그래밍&topK=5"
```

### 고급 기능 테스트

#### 5. 메타데이터와 함께 문서 추가

```bash
curl -X POST http://localhost:9000/api/vectordb/advanced/add-with-metadata \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Kotlin은 JVM 언어입니다.",
    "metadata": {"category": "programming", "language": "kotlin"},
    "id": "doc-kotlin-001"
  }'
```

#### 6. 메타데이터 필터링 검색

```bash
curl -X POST http://localhost:9000/api/vectordb/advanced/search-with-filter \
  -H "Content-Type: application/json" \
  -d '{
    "query": "프로그래밍",
    "topK": 5,
    "metadataFilter": {"category": "programming"}
  }'
```

#### 7. 서비스를 통한 문서 추가

```bash
curl -X POST http://localhost:9000/api/vectordb/service/add \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Spring Boot는 자바 프레임워크입니다.",
    "metadata": {"category": "framework"}
  }'
```

#### 8. 서비스를 통한 검색

```bash
curl -X POST http://localhost:9000/api/vectordb/service/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "프레임워크",
    "topK": 3
  }'
```

#### 9. 문서 통계 조회

```bash
curl http://localhost:9000/api/vectordb/service/stats
```

## 🔍 디버깅

### 로그 확인

애플리케이션 실행 시 로그를 확인하여 디버깅할 수 있습니다:

```yaml
# application.yml에 이미 설정됨
logging:
  level:
    com.example.springai: DEBUG
    org.springframework.ai: DEBUG
```

## ❌ 문제 해결

### 문제 1: SimpleVectorStore 데이터 손실

**증상:**
```
서버 재시작 후 데이터가 사라짐
```

**원인**: SimpleVectorStore는 메모리 기반

**해결책:**
- 프로덕션에서는 PGVector 등 영구 저장소 사용
- 개발 환경에서는 정기적으로 백업

### 문제 2: 검색 결과가 예상과 다름

**증상:**
```
유사한 문서가 검색되지 않음
```

**해결책:**
- 임베딩 모델 확인
- topK 값 조정
- 검색 쿼리 개선

### 문제 3: API 응답이 느림

**증상:**
```
검색 요청이 느림
```

**원인**: 임베딩 생성 시간

**해결책:**
- 임베딩 캐싱 고려
- 배치 처리 활용

## ✅ 체크리스트

실행 전 확인사항:

- [ ] JDK 17 이상 설치됨
- [ ] OpenAI API Key 발급됨
- [ ] 환경 변수 설정됨
- [ ] 프로젝트 빌드 성공 (`./gradlew build`)
- [ ] 애플리케이션 실행 성공 (`./gradlew bootRun`)
- [ ] API 엔드포인트 응답 확인

## 🎓 학습 권장 순서

1. **기본 문서 추가**: `/api/vectordb/add`
2. **유사도 검색**: `/api/vectordb/search`
3. **배치 문서 추가**: `/api/vectordb/batch/add`
4. **고급 기능**: `/api/vectordb/advanced/*`
5. **서비스 기반 사용**: `/api/vectordb/service/*`
6. **코드 분석**: VectorStoreConfig의 SimpleVectorStore 설정 확인

## 📚 핵심 개념 정리

### VectorStore 설정 패턴

```kotlin
@Configuration
class VectorStoreConfig(
    private val embeddingModel: EmbeddingModel
) {
    @Bean
    fun vectorStore(): VectorStore {
        return SimpleVectorStore.builder(embeddingModel).build()
    }
}
```

### 문서 추가 패턴

```kotlin
val document = Document("문서 내용", metadata)
vectorStore.add(listOf(document))
```

### 유사도 검색 패턴

```kotlin
val results = vectorStore.similaritySearch(query) ?: emptyList()
val limitedResults = results.take(topK)
```

### Document 접근

```kotlin
// Document의 내용은 text 속성으로 접근
doc.text        // 문서 내용
doc.metadata   // 메타데이터
doc.id         // 문서 ID (nullable)
```

---

**다음 학습**: [6.2: Spring AI VectorStore 추상화 (In-Memory)](../../README.md#62-spring-ai-vectordb-추상화-in-memory)

