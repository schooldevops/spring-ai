spring:
  application:
    name: chatmemory-conversation-management-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
server:
  port: 9503
logging:
  level:
    org.springframework.ai: DEBUG
