spring:
  application:
    name: chatmemory-message-window-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
server:
  port: 9501
logging:
  level:
    org.springframework.ai: DEBUG
