spring:
  application:
    name: chatmemory-chatclient-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
server:
  port: 9502
logging:
  level:
    org.springframework.ai: DEBUG
