spring:
  application:
    name: chatmodel-chat-options-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
server:
  port: 9203
logging:
  level:
    org.springframework.ai: DEBUG
