spring:
  application:
    name: function-weather-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
server:
  port: 9601
logging:
  level:
    org.springframework.ai: DEBUG
