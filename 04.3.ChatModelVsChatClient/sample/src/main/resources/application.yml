spring:
  application:
    name: chatmodel-vs-chatclient
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: gemma2:2b
          temperature: 0.7

server:
  port: 8080

logging:
  level:
    org.springframework.ai: DEBUG
