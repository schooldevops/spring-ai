spring:
  application:
    name: chatclient-prompt-templates-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
server:
  port: 9104
logging:
  level:
    org.springframework.ai: DEBUG
